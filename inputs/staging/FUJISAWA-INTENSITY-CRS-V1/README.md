# T-B source evidence and current binding

`crs_closure_receipt.json` is the unchanged historical FABLE input (2026-09-11).
Its `connect_now=false` and pending human items describe that input checkpoint,
not the current implementation. The original source timestamp is not changed.
Current implementation mapping is
`reports/FUJISAWA_INTENSITY_CRS_CLOSURE_RECEIPT.json`.
This report-level mapping does not add an artifact to the frozen citypack inventory.

`license_binding.json` is a new 2026-09-12 retrospective resource metadata check.
It binds only resource `704a2ee0-0040-4a96-b92a-c8e36b559d3d`, retained `01_.zip`
SHA and its eight intensity-scenario derivatives. The provider API has no hash;
this does not claim a provider-published SHA or a fresh archive download.
The raw original remains outside Git at root ID `EXTERNAL_OFFICIAL_DATA_ROOT`.
Only specifically hash-bound inputs may be passed to the existing generator.

The current resource page links CC BY 4.0. The terms require attribution,
modification notice, and respect for third-party rights; no endorsement is implied.
This does not grant Fujisawa city facility permission or resolve public history.
The definition workbook supplies field definitions, not a direct CRS correction.
The source receipt's sibling declaration/mesh correspondence is limited to 01_.
No raw, workbook original, website response body, or local path is tracked here.
