# Run retrospective

The dependency-first fan-out produced green city, core, CI, Hokonavi, and UI lanes quickly enough to preserve the integration reserve. The two meaningful integration misses were both cross-lane: Python package identity and a closed hazard enum. A narrow cross-pack collection/schema gate would have found both earlier.

The first evaluator draft overstated safety gates. Independent LUNA/TERA review prevented adoption until worktree/index/protected blobs and post-repair full suites were measured. This is evidence that the human/auditor boundary is functioning, not that the workflow can safely self-promote.

The 2D-first cutover was correct. No trustworthy 3D tileset or real geometry was available, so the viewer exposes a recoverable `NOT_IMPLEMENTED` panel instead of a false layer. The next run should consume human-approved real geometry and facility/operation evidence before expanding visualization.

Permanent strategy changes, any push/PR, and any merge to `main` remain human-gated.
